{
  "name": "sass",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm-run-all --parallel devserver sass",
    "sass": "node-sass -w scss/style.scss css/style.css",
    "devserver": "live-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Agata",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^7.0.1",
    "run-all": "^1.0.1"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
